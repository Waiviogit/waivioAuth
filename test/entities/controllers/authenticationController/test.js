const { chai, chaiHttp, app, models, Mongoose, api_prefix } = require( '../../../testHelper' );
const { tokenFactory } = require( '../../../factories/index' );

chai.use( chaiHttp );
chai.should();
const expect = chai.expect;


describe( 'Authorization', async () => {

    before( async() => {

    } );

    describe( 'sign in', async () => {


    } );
} );
